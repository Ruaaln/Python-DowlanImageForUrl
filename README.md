## Image Downloader

A simple Python script to download images from a URL.  
- Saves images to a `Download` folder in the same directory as the script.  
- Enter `exit` or `quit` to stop the program.  

**Usage:**

```bash
python main.py
